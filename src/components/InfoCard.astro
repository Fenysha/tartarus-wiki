---
const { title = 'Блок данных', icon = 'document', body = 'Описание блока.' } = Astro.props;
---

<article class="info-card">
  <div class="info-card__icon">{icon}</div>
  <div>
    <h3>{title}</h3>
    <p>{body}</p>
  </div>
</article>

<style is:global>
  .info-card {
    display: flex;
    gap: 1rem;
    padding: 1rem 1.1rem;
    background: #111;
    border: 0;
    box-shadow: inset 1px 0 0 var(--sl-color-gray-2);
  }

  .info-card__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    background: #252525;
    color: var(--sl-color-white);
    font: 700 0.75rem var(--sl-font-mono);
  }

  .info-card h3 {
    margin: 0 0 0.35rem;
    font: 700 0.85rem var(--sl-font-mono);
    color: var(--sl-color-text-accent);
  }

  .info-card p {
    margin: 0;
    color: var(--sl-color-text);
    opacity: 0.9;
  }
</style>
